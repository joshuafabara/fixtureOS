import { PageStub } from "@/components/shared/page-stub";
import { MessageSquare } from "lucide-react";
export default function CommunicationsPage() {
  return <PageStub title="Directorio de Comunicaciones" description="Estado de contactos WhatsApp por torneo y categoría. Envío de mensajes en fase futura (Evolution API)." Icon={MessageSquare} />;
}
