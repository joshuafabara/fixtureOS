import { PageStub } from "@/components/shared/page-stub";
import { GitCompare } from "lucide-react";
export default function FixtureComparePage() {
  return <PageStub title="Comparar Versiones" description="Compara dos versiones de fixture lado a lado con diffs visuales." Icon={GitCompare} />;
}
