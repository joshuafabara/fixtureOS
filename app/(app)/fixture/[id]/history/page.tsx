import { PageStub } from "@/components/shared/page-stub";
import { History } from "lucide-react";
export default function FixtureHistoryPage() {
  return <PageStub title="Historial de Versiones" description="Consulta y compara versiones anteriores del fixture." Icon={History} />;
}
