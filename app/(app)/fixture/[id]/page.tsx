import { PageStub } from "@/components/shared/page-stub";
import { Calendar } from "lucide-react";
export default function FixturePage() {
  return <PageStub title="Fixture Viewer" description="Visualiza y publica fechas de fixture por versión y categoría." Icon={Calendar} />;
}
