import { PageStub } from "@/components/shared/page-stub";
import { Building2 } from "lucide-react";
export default function ClubDetailPage() {
  return <PageStub title="Detalle de Club" description="Ver equipos por categoría, contactos y números de WhatsApp del club." Icon={Building2} />;
}
