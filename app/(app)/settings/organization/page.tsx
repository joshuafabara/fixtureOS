import { PageStub } from "@/components/shared/page-stub";
import { Settings } from "lucide-react";
export default function OrgSettingsPage() {
  return <PageStub title="Configuración de Organización" description="Nombre, idioma, conexión Drupal, duración predeterminada de partidos." Icon={Settings} />;
}
