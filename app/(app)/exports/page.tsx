import { PageStub } from "@/components/shared/page-stub";
import { Download } from "lucide-react";
export default function ExportsPage() {
  return <PageStub title="Centro de Exportación" description="Exporta el fixture en Excel, PDF o imagen. Filtra por torneo, versión y categoría." Icon={Download} />;
}
