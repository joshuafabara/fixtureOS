import { PageStub } from "@/components/shared/page-stub";
import { ClipboardList } from "lucide-react";
export default function StandingsReviewPage() {
  return <PageStub title="Revisar Posiciones" description="Confirma los equipos detectados antes de generar el fixture de playoffs." Icon={ClipboardList} />;
}
