import { PageStub } from "@/components/shared/page-stub";
import { ClipboardList } from "lucide-react";
export default function StandingsImportPage() {
  return <PageStub title="Importar Posiciones" description="Carga posiciones manualmente, desde imagen o Excel para generar llaves de playoffs." Icon={ClipboardList} />;
}
